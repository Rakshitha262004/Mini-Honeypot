# Low-Interaction-Honeypot
Developed a Multi-Service Low-Interaction Honeypot using Python that simulates SSH and HTTP services, captures attacker login attempts, logs IP address, username, password, and timestamp into a JSON file, and displays attack activity on a Flask-based monitoring dashboard.
